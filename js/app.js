define(['jquery', 'weather/weathermodel'], function ($, WeatherModel) {
	'use strict';

	var application = {
		initialize: function () {
			console.log('initialized');
		}
	};

	return application;
});